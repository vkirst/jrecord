version = '0.85.1'

dependencies {
    compile project(':JRecord')

    compile group: 'org.apache.velocity', name: 'velocity', version: '1.7'
}

jar() {
    manifest {
        attributes 'Implementation-Title': 'JRecord codegen module',
                'Implementation-Version': version
    }
}
