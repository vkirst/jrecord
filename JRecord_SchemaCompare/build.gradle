ext.versionStr = '0.85.1'

dependencies {
    compile project(':JRecord')
}

jar() {
    manifest {
        attributes 'Implementation-Title': 'JRecord schema compare module',
                'Implementation-Version': versionStr
    }
}